#!/usr/bin/env sh

set -e

case $@ in
  help|-h|--help)
    cat $PWD/man/m.test.1
    exit 0
  ;;
  version|-v|--version)
    echo $npm_package_version
    exit 0
  ;;
esac

find ${@:-test} -type f | xargs -n 1 node --require ../globals
