
  Usage m.test [options] [files]

  Commands

    help                     output usage information
    version                  output the version number

  Options

    -h, --help                 output usage information.
    -v, --version              output the version number.
    -p, --parallel "maxprocs"  parallel mode: run at most `maxprocs`
                               invocations of suites at once.
    -r, --require "module"     preload  the  specified  module at startup.
                               follows `require` module resolution rules.
                               module may  be either  a  path to a file,
                               or a node module name.
